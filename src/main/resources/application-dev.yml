spring:
  datasource:
    # Docker MySQL Connection
    url: jdbc:mysql://localhost:3306/bakery_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    # JPA Dev Settings
    show-sql: true
    hibernate:
      ddl-auto: update